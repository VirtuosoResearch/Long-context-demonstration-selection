# Instruction

Follow the setup instruction in the `webshop` folder to install and launch the WebShop environment

Use `eval_webshop.py` to evaluate a language agent
- Specify `model_name` to use a specific language model 

